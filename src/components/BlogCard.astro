---
import { Image } from "astro:assets";

const { title, excerpt, img, caption, author, authorSlug, readingTime, slug } =
  Astro.props;
---

<article
  class="flex flex-col w-full h-full overflow-hidden rounded-lg shadow-md gap-4"
>
  <Image
    class="object-cover w-full h-56 rounded-b-lg shadow-md"
    src={img}
    alt={caption}
    height={300}
    width={300}
    loading="lazy"
  />

  <header class="px-4">
    <h3 class="text-lg font-bold text-center text-pretty text-neutral-800">
      {title}
    </h3>
  </header>

  <div class="px-4">
    <div class="text-clamp-4 [&>p]:text-neutral-600" set:html={excerpt} />
  </div>

  <footer class="px-3 pb-6">
    <div class="mb-6 text-sm">
      <a href=`/blog/autor/${authorSlug}` class="font-semibold text-primary/80">
        {author}
      </a>
      <span>
        - Lectura:
        {readingTime}
      </span>
    </div>
    <a
      class="block px-6 py-3 text-white rounded-lg w-fit bg-primary hover:bg-dark-primary"
      href={`/blog/${slug}`}>Leer más</a
    >
  </footer>
</article>
